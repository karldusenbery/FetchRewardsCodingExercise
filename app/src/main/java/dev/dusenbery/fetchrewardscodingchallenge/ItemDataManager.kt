package dev.dusenbery.fetchrewardscodingchallenge

import android.content.Context

class ItemDataManager(private val context: Context) {

    fun readItems(): ArrayList<ItemList> {
        val itemsList = ArrayList<ItemList>()

        return itemsList
    }
}